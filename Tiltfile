# Welcome to GovCMS!
print("""
-----------------------------------------------------------------
✨ Hello GovCMS!
-----------------------------------------------------------------
""".strip())

# include sub sites.
include('./api/Tiltfile')

# Good bye.
if config.tilt_subcommand == 'down':
    print('Goodbye world!')
